'use strict';

/**
 * This file is needed, because we use `mukla`
 * and because we don't have CLI for it.
 * So we need to require all of our test files manually.
 */

require('./api.test');
require('./utils.test');
